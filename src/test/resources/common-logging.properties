#if commons log jar found in class path, will use it, otherwise will use JDK logger.

#if found commons log jar, this configuration file will effect  

#Below line tell commons log to load Log4j
org.apache.commons.logging.Log=org.apache.commons.logging.impl.Log4JLogger
